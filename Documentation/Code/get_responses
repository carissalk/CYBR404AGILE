def get_response(user_input: str) -> str:
    lowered: str = user_input.lower()

    if lowered == '':
        return 'Say something'
    elif 'hello' or 'hi' in lowered:
        return 'Hello there!'
    elif 'say something' in lowered:
        return 'This is different'
